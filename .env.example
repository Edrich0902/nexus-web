VITE_API_BASE_URL=http://api.nexus.test

# Cloudinary cloud name only (delivery URLs). Secrets stay on nexus-api.
VITE_CLOUDINARY_CLOUD_NAME=

# Food & Drink keys live on the API (.env) — the web never calls WineAPI / MealDB / OBDB directly.
